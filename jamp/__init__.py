"""jamp: rename, tag and check a live music archive without destroying anything.

Phase 0 inventories, phase 1 plans, phase 2 writes (only with --commit),
phase 3 fills tags from reference sources, and `complete` asks whether a
recording is missing songs.  See README.md.
"""

__version__ = "0.2.0b2"
