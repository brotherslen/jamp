__pycache__/
*.pyc
.pytest_cache/
# Reports are regenerated by every run and live outside the project anyway.
/reports/

# Phase 3 cache - a local copy of what archive.org knows, not source.
cache/

# The phish.net key lives outside the repo, but guard against a stray copy.
*.key

# Remembered paths are per-installation, not part of the project.
config/etree.paths.json

# Your own answers live in the user folder; guard against a stray copy here.
config/overrides.yaml

# Packaging leftovers from pip install.
build/
dist/
*.egg-info/
